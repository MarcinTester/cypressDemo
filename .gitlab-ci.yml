image: cypress/base:14

stages:
  - test

cypress-tests:
  stage: test
  script:
    - npm install 
    - npx cypress run --headless --browser chrome

  artifacts:
    paths:
      - cypress/screenshots
    expire_in: 1 day